backend:
  name: git-gateway
  branch: master

media_folder: "static/upload"
public_folder: "upload"

collections:
  - name: "posts"
    label: "Post"
    folder: "_posts"
    filter:
      field: language
      value: en
    fields:
      - {label: "Language", name: "language"}